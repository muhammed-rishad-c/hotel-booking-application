<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Orders</title>
    <!-- Bootstrap CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #000;
            color: #eee;
            margin: 0;
            padding: 0;
        }

        .navbar {
            background-color: #111;
            padding: 1.5rem; /* Increased padding for larger navbar */
        }

        .navbar-brand {
            color: #fff !important;
            font-size: 1.5rem; /* Increased font size for larger navbar */
            transition: color 0.3s, transform 0.3s;
        }

        .navbar-brand:hover {
            color: #ffc107 !important;
            transform: scale(1.1);
        }

        .nav-link {
            color: #fff !important;
            font-size: 1.3rem; /* Increased font size for larger navbar */
            transition: color 0.3s;
        }

        .nav-link:hover {
            color: #ffc107 !important;
        }

        .container {
            margin-top: 2rem;
            color: #eee;
        }

        .order-card {
            background-color: #222;
            border: 1px solid #333;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 2rem;
            padding: 1rem;
            display: flex;
            flex-direction: column;
            gap: 1rem;
            color: #eee;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .order-card:hover {
            transform: scale(1.02);
            box-shadow: 0 8px 16px rgba(255, 255, 255, 0.2);
        }

        .order-card h3 {
            color: #ffc107;
        }

        .order-card p {
            margin: 0.25rem 0;
        }

        .no-orders {
            text-align: center;
            margin-top: 2rem;
            color: #bbb;
        }

        /* Add btn-upload class as in the previous example */
        .btn-upload {
            padding: 0.5rem 1rem;
            font-size: 1rem;
            color: #fff;
            background-color: #111;
            border: none;
            border-radius: 5px;
            transition: background-color 0.3s, transform 0.3s;
        }

        .btn-upload:hover {
            background-color: #ffc107;
            color: #111;
            transform: scale(1.05);
        }

        /* Ensure links are styled consistently */
        a {
            color: inherit;
            text-decoration: none;
        }

        a:hover {
            color: #ffc107;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <a class="navbar-brand" href="#">Admin Dashboard</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/admin/admin-index">Home</a>
                </li>
            </ul>
            <form class="form-inline my-2 my-lg-0">
                <a href="/admin/upload" class="btn btn-upload mr-2">Upload Post</a>
                <a href="/admin/orders" class="btn btn-upload mr-2">Check Order</a>
                <a href="/admin/logout" class="btn btn-upload">Logout</a>
            </form>
        </div>
    </nav>

    <div class="container">
        <% if (orders.length === 0) { %>
        <div class="no-orders">No orders are here.</div>
        <% } else { %>
        <% orders.forEach(order => { %>
        <div class="order-card">
            <h3>Order ID: <a href="/admin/order/<%= order.booking_id %>" style="color: #ffc107;"><%= order.booking_id %></a></h3>
            <p><strong>First Name:</strong> <%= order.first_name %></p>
            <p><strong>Last Name:</strong> <%= order.last_name %></p>
            <p><strong>Hotel ID:</strong> <%= order.hotel_id %></p>
            <p><strong>Booking Date:</strong> <%= order.created_at %></p>
            <p><strong>Check-in Date:</strong> <%= order.check_in_date %></p>
            <p><strong>Check-out Date:</strong> <%= order.check_out_date %></p>
        </div>
        <% }); %>
        <% } %>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
